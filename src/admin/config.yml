backend:
  name: git-gateway
  repo: 'imbalacoop/imbala'
  squash_merges: true

site_url: 'https://imbala.community'

publish_mode: editorial_workflow
media_folder: '/images/uploads'

collections:
collections:
  - name: 'events' # Used in routes, e.g., /admin/collections/blog
    label: 'Events' # Used in the UI
    folder: 'site/_data/events' # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: '{{year}}-{{month}}-{{title}}' # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: 'Title', name: 'title', widget: 'string' }
      - { label: 'URL', name: 'event_url', widget: 'string' }
      - { label: 'Start Date', name: 'date', widget: 'datetime' }
      - { label: 'Description', name: 'description', widget: 'markdown' }

#   - name: 'menu'
#     label: 'Menu'
#     folder: 'site/_data/menu.json'
#     extension: 'json'
#     create: true
#     delete: false
#     editor:
#       preview: false

#     files:
#       - name: 'nav'
#         label: 'Navigation'
#         file: 'src/site/_data/nav.json'
#         description: `Nav`
#         fields:
#           - label: 'English'
#             name: 'en'
#             widget: 'object'
#             fields:
#               - { label: 'About', name: 'about', widget: 'string' }
#               - { label: 'Cafe', name: 'cafe', widget: 'string' }
#               - { label: 'Join', name: 'join', widget: 'string' }
#               - { label: 'Address', name: 'address', widget: 'string' }
