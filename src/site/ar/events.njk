---
title: 'أحداث'
backgroundImage: '/images/events-bg.jpg'
---

{% extends 'layouts/page.njk' %} {% block pageContent %}

  {% if collections.events.futureEvents.length > 0 %} <div class="events">{% for event in collections.events.futureEvents %}

  <div class="event-box-wrapper {{ event.data.background }}-bg">
    <div class="event-box">
      <div class="event-box__date">
        {{
          event.data.date.toLocaleDateString('ar-IL', {
            weekday: 'long',
            month: 'short',
            day: 'numeric'
          })
        }},
        {{ event.data.date.toLocaleTimeString('ar-IL', { hour: '2-digit', minute: '2-digit' }) }}
      </div>
      <h3 class="event-box__title">{{ event.data.title_ar }}</h3>
      <div class="event-box__description">
        {{ event.data.description_ar }}
      </div>
      {% if event.data.event_url %}
      <a href="{{ event.data.event_url }}" target="_blank">حدث فيسبوك</a>
      {% endif %}
    </div>
  </div>
  {% endfor %} </div>{% endif %} 
  <h3 class="event-page-subheader">الأحداث السابقة</h3>
  <div class="events">
{% for event in collections.events.pastEvents %}
  <div class="event-box-wrapper {{ event.data.background }}-bg">
    <div class="event-box">
      <div class="event-box__date">
        {{
          event.data.date.toLocaleDateString('ar-IL', {
            weekday: 'long',
            month: 'short',
            day: 'numeric'
          })
        }},
        {{ event.data.date.toLocaleTimeString('ar-IL', { hour: '2-digit', minute: '2-digit' }) }}
      </div>
      <h3 class="event-box__title">{{ event.data.title_ar }}</h3>
      <div class="event-box__description">
        {{ event.data.description_ar }}
      </div>
      {% if event.data.event_url %}
      <a href="{{ event.data.event_url }}" target="_blank">حدث فيسبوك</a>{% endif %} 
    </div>
</div>{% endfor %}</div>
{% endblock %}
